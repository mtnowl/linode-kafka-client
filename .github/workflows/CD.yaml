name: Continuous Deployment

on:
  workflow_dispatch:

env:
  SSH_KEY_PATH: devops/ansible/private.pem
  TF_VARS_token: ${{secrets.TERRAFORM_ACCESS_TOKEN}}
  TF_VARS_authorized_keys: ${{secrets.TERRAFORM_AUTHORIZED_KEYS}}
  TF_VARS_root_pass: ${{secrets.TERRAFORM_ROOT_PASS}}
  AWS_ACCESS_KEY_ID: ${{secrets.TERRAFORM_S3_ACCESS_KEY}}
  AWS_SECRET_ACCESS_KEY: ${{secrets.TERRAFORM_S3_SECRET_ACCESS_KEY}}
  :

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
      - name: Terraform Init
        run: terraform -chdir=./devops/terraform init
      - name: Terraform Validate
        run: terraform -chdir=./devops/terraform validate -no-color
      - name: Terraform Apply Changes
        run: terraform -chdir=./devops/terraform apply -auto-approve
  ansible:
    needs: terraform
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install Ansible
        run: pip install ansible
      - name: Create private SSH Key
        run: |
          cat << EOF > $SSH_KEY_PATH
          ${{ secrets.ANSIBLE_PRIVATE_SSH_KEY }}
          EOF
      - name: Update key permissions
        run: |
          chmod 400 $SSH_KEY_PATH
      - name: Run main playbook
        run: |
          cd devops/ansible
          ansible-playbook --private-key $SSH_KEY_PATH main.yaml
